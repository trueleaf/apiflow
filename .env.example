# .env.example - Example environment variables and deployment guide
#
# Notes:
# - Copy this file to the project root and name it `.env` (do NOT commit your real .env to Git)
# - In production, use strong secrets and store them in your platform's secret manager (Docker secrets / K8s secrets / CI/CD secrets)
# Security recommendations:
# - Use unique accounts and strong passwords; enable backups and access control
# - Keep secrets in a secret manager (do not hardcode)
# - Run containers with least privilege and consider read-only root file systems
#
# Common variables (examples):
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=your_secure_password_here
MONGO_DATABASE=apiflow

# NPM Registry Configuration
# Set to 'true' to use npm mirror (npmmirror.com), 'false' to use default registry
USE_NPM_MIRROR=false
# 请替换为你自己的 API Key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1/chat/completions
DEEPSEEK_MODEL=deepseek-chat