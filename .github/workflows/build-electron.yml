name: Build Electron

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (例如: v1.0.0)'
        required: false
        default: 'manual-build'

jobs:
  test-web:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装依赖
        run: npm ci --include=optional
        env:
          npm_config_ignore_optional: true

      - name: 安装 Linux 构建依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libgtk-3-0 libgbm1 libnss3 libxss1 \
          libxtst6 libnotify4 libatspi2.0-0 libdrm2 libxcb-dri3-0 libx11-xcb1

      - name: 构建 Electron 应用（主 + 渲染）
        run: npm run web:build:app:pack

  build-windows:
    needs: [test-web]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装依赖
        run: npm ci --include=optional

      - name: 打包 Windows 应用（自动上传 Release）
        run: npm run web:build:app:win
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          npm_config_target_platform: win32

  build-linux:
    needs: [test-web]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装依赖
        run: npm ci --include=optional

      - name: 打包 Linux 应用（自动上传 Release）
        run: npm run web:build:app:linux
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          npm_config_target_arch: x64
          npm_config_target_platform: linux

  build-macos:
    needs: [test-web]
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装依赖
        run: npm ci --include=optional

      - name: 构建 macOS 应用（自动上传 Release）
        run: npm run web:build:app:mac
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
          npm_config_target_platform: darwin
          # 避免 electron-builder 对未解决依赖报错
          ELECTRON_BUILDER_ALLOW_UNRESOLVED_DEPENDENCIES: true

  finalize-release:
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - name: 修改 Draft Release 内容（不新建 Release）
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          update_release: true
          body: |
            ## 📦 下载包已经全部构建完成
            Release 内容已经由 electron-builder 自动上传。

            ✔ Windows  
            ✔ macOS  
            ✔ Linux  

            请检查文件后再点击 **Publish release** 发布版本。
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
