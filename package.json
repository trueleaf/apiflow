{
  "name": "apiflow",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web:dev": "npm run dev -w apiflow",
    "web:web": "npm run web -w apiflow",
    "web:share": "npm run share -w apiflow",
    "web:build": "npm run build:web -w apiflow",
    "web:build:share": "npm run build:share -w apiflow",
    "web:build:web": "npm run build:web -w apiflow",
    "web:build:font": "npm run build:font -w apiflow",
    "web:preview": "npm run preview -w apiflow",
    "web:type-check": "npm run type-check -w apiflow",
    "server:dev": "npm run dev -w @apiflow/server",
    "server:start": "npm run start -w @apiflow/server",
    "server:test": "npm run test -w @apiflow/server",
    "server:cov": "npm run cov -w @apiflow/server",
    "server:lint": "npm run lint -w @apiflow/server",
    "server:lint:fix": "npm run lint:fix -w @apiflow/server",
    "server:ci": "npm run ci -w @apiflow/server",
    "server:build": "npm run build -w @apiflow/server",
    "dev": "npm run dev -w apiflow",
    "web:build:app": "npm run build:app -w apiflow",
    "web:build:app:pack": "npm run build:app:pack -w apiflow",
    "web:build:app:win": "npm run build:app:win -w apiflow",
    "web:build:app:mac": "npm run build:app:mac -w apiflow",
    "web:build:app:linux": "npm run build:app:linux -w apiflow"
  },
  "devDependencies": {
    "@types/koa-bodyparser": "^4.3.12",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
