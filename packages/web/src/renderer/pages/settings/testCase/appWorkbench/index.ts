import { type ModelNode } from '../types'
import child0 from './workbenchTop'

const node: ModelNode = {
  modelName: "appWorkbench",
  description: "应用工作区",
  children: [child0],
}

export default node
