import { type ModelNode } from '../../types'
import child0 from './globalCommonHeaders'

const node: ModelNode = {
  modelName: "commonHeader",
  description: "公共请求头",
  children: [child0],
}

export default node
