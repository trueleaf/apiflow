import { type ModelNode } from '../../../types'
import child0 from './nodeOperation'
import child1 from './callHistory'

const node: ModelNode = {
  modelName: "bannerDetails",
  description: "banner详情区",
  children: [child0, child1],
}

export default node
