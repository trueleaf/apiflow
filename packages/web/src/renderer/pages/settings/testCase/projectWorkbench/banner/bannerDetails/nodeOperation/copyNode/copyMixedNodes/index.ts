import { type ModelNode } from '../../../../../../types'
import child0 from './copyMixedNodeTypes'

const node: ModelNode = {
  modelName: "copyMixedNodes",
  description: "复制混合节点",
  children: [child0],
}

export default node
