import { type ModelNode } from '../../../../../../types'
import child0 from './cutMixedNodeTypes'

const node: ModelNode = {
  modelName: "cutMixedNodes",
  description: "剪切混合节点",
  children: [child0],
}

export default node
