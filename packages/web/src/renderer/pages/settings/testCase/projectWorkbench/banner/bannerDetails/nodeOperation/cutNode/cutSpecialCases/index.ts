import { type ModelNode } from '../../../../../../types'
import child0 from './cutWithoutPaste'

const node: ModelNode = {
  modelName: "cutSpecialCases",
  description: "剪切操作特殊场景",
  children: [child0],
}

export default node
