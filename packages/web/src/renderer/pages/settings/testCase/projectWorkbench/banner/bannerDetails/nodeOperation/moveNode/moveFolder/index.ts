import { type ModelNode } from '../../../../../../types'
import child0 from './moveSingleFolder'

const node: ModelNode = {
  modelName: "moveFolder",
  description: "移动folder节点",
  children: [child0],
}

export default node
