import { type ModelNode } from '../../../../../../types'
import child0 from './moveSingleHttpMockNode'

const node: ModelNode = {
  modelName: "moveHttpMockNode",
  description: "移动httpMockNode节点",
  children: [child0],
}

export default node
