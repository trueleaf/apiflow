import { type ModelNode } from '../../../../../../types'
import child0 from './moveSingleHttpNode'

const node: ModelNode = {
  modelName: "moveHttpNode",
  description: "移动httpNode节点",
  children: [child0],
}

export default node
