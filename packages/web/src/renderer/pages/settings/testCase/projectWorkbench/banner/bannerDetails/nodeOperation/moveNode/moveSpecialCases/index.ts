import { type ModelNode } from '../../../../../../types'
import child0 from './movePerformance'

const node: ModelNode = {
  modelName: "moveSpecialCases",
  description: "移动操作特殊场景",
  children: [child0],
}

export default node
