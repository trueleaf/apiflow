import { type ModelNode } from '../../../../../../types'
import child0 from './moveSingleWebsocketMockNode'

const node: ModelNode = {
  modelName: "moveWebsocketMockNode",
  description: "移动websocketMockNode节点",
  children: [child0],
}

export default node
