import { type ModelNode } from '../../../../../../types'
import child0 from './moveSingleWebsocketNode'

const node: ModelNode = {
  modelName: "moveWebsocketNode",
  description: "移动websocketNode节点",
  children: [child0],
}

export default node
